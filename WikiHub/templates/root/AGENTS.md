# {{TITLE}} Agent Protocol

- Read `index.md` before answering questions.
- Reuse `wiki/` pages first instead of revisiting `raw/` for every answer.
- Put new sources into `raw/` first, then update `wiki/`.
- Make sure every important conclusion can be traced back to a source.
- Mark conflicts and uncertainty explicitly.
